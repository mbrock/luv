#+title: Luv workbench substrate worksheet
#+startup: overview

* Intent and non-goals
:PROPERTIES:
:ID: Z96DF9
:END:

This is the living worksheet for the screen-space application substrate
described by #M9MI6Y.  The architecture record says why the substrate exists
and which decisions constrain it; this page sequences experiments, records
what they teach, and names exactly one next action.  Execution threads are
replaceable working sessions.  They read and update this worksheet rather than
becoming the only place where an architectural decision survives.

The intended result is one Luv-owned workbench over any conforming graphical
application: compact status, lobby, metabar, M-x, source review, and later
presentation-typed prompts share one technical visual language and one model
of panes, focus, modality, input, rendering, and release.  Their value comes
from being available without an atelier inventing a host adapter for every
tool.  Luft is the first conforming atelier.

This work does /not/ define a common game engine, absorb Luft's site inspector
or authored tools into global chrome, generalize Luvcraft's spatial overlays,
or preserve arbitrary third-party extension registration.  Luvcraft migration
is deferred until Luft proves the substrate.  Its scene, world-panel,
viewmodel, and world-focus machinery may remain independent even when a later
fixed-screen workbench supersedes it temporarily.

* Established invariants
:PROPERTIES:
:ID: H51JQ0
:END:

- Workbench state and GPU-resource mutation cross the native frame boundary.
- Asynchronous work quiesces before a terminal canvas fence.  A source fetch
  may be cancelled; a checkout mutation and live ASDF assimilation complete
  before release.
- Native input reaches the shell before application input.  Passive,
  modeless, transient, and modal panes have explicit behavior rather than
  numeric event priority.
- Opening interactive shell UI clears held application input before swallowing
  corresponding release events and suspends relative pointer capture through
  an explicit application token.
- McCLIM authors retained semantic commands.  The shell does not rasterize a
  pane texture, acquire a drawable, submit a queue, or own an application
  renderer.
- Shell resources and workers are released while the context and GPU device
  remain live; application renderer resources, canvas, and device retain their
  existing terminal order.
- Application stop closes shell admission atomically.  Startup or publication
  which loses that race consumes and releases its own resources.
- The application supplies semantic values and commands, not per-pane
  compositors, placement matrices, inverse pointer transforms, or open/close
  adapters.

* Current architecture
:PROPERTIES:
:ID: 8VVBDJ
:END:

The current implementation is the evidence and migration source, not the
desired boundary.

- [[file:../hal/canvas.lisp][hal/canvas.lisp]] owns the portable presentation transaction.
  =PRESENT-CANVAS-FRAME= acquires a surface and encoder, invokes the
  application, then completes presentation.  A shell postlude here could
  composite after an application without entering its renderer.
- [[file:../hal/sdl/canvas.lisp][hal/sdl/canvas.lisp]] owns the native SDL event and frame loop.  It is the
  natural outer boundary for shell-first input and frame-boundary requests.
- [[file:../mcclim/direct-compositor.lisp][mcclim/direct-compositor.lisp]] already replays retained McCLIM commands into
  a caller-owned render pass.
- [[file:../mcclim/gpu.lisp][mcclim/gpu.lisp]] already combines descendant sheet streams in painter order.
  The first proof must establish that overlapping child panes preserve clips,
  transforms, and native-density text in one embedded frame.
- [[file:../mcclim/mirror.lisp][mcclim/mirror.lisp]] translates native events through sheets, but embedded
  top-level mirrors do not yet expose ordinary viewport resize and direct
  canvas-event dispatch as a clean public protocol.
- [[file:../luft/render/instruments.lisp][luft/render/instruments.lisp]] is a small window manager and lifecycle registry:
  numeric visual/input order, refresh, encoding, event interception,
  quiescence, and release.  Status, lobby, metabar, M-x, and source review use
  it visually; Tracy and the application agent use it invisibly.
- [[file:../luft/render/studio.lisp][luft/render/studio.lisp]] owns the first conforming application, its renderer,
  ordinary atelier commands, site inspector, input state, and terminal release
  order.  Those application concerns remain Luft's.

The applications-by-tools adapter matrix and the focused-pointer routing bug
in the current Luvcraft M-x/source panels are deletion pressure, not contracts
to reproduce.

* Open technical questions
:PROPERTIES:
:ID: PMDM1O
:END:

1. Can one transparent full-viewport embedded McCLIM frame retain and replay
   overlapping child panes with correct logical resize, clipping, painter
   order, and native-density rendering?
2. What is the smallest public operation for resizing an embedded top-level
   mirror and laying out its children after an SDL logical-size change?
3. Can canvas events enter that mirror's normal sheet dispatch without making
   it the canvas's sole native handler?
4. Should the portable presentation transaction own exactly one
   =APPLICATION-SHELL= postlude, or does backend behavior require the
   application to call the shell inside its final pass?  Prefer the postlude;
   decide from the backend proof rather than aesthetics.
5. Is shell chrome included in application screenshots and films, excluded
   like browser devtools, or selectable?  Do not let this choice complicate
   the first on-screen proof.
6. Which application-input suspension token is sufficient for Luft without
   importing Luvcraft camera/world-focus semantics?
7. How will incremental presentation-typed prompting replace blocking CLIM
   =ACCEPT= on the canvas thread?  This is downstream of basic modal routing.

* Ordered stages
:PROPERTIES:
:ID: CEIG07
:END:

The stages are gates, not a backlog to execute speculatively.  An execution
thread owns one stage, records discoveries and decisions here, leaves
reviewable unpushed changes, writes a prose completion update, and identifies
rather than begins the next stage.

** DONE Prove the embedded workbench backend
:PROPERTIES:
:ID: BY51K0
:END:

/Intent./ Build the smallest transparent full-viewport embedded frame with two
overlapping child panes.  Exercise resize, child layout, clipping, painter
order, native-density retained drawing, pointer hit testing, keyboard focus,
and dispatch from portable canvas events.

/Evidence./ Descendant retained-stream composition and direct replay already
exist, while embedded mirror resize and public event entry are unresolved.
No production shell or migrated tool is needed to answer those questions.

/Done when./ Focused executable checks and one inspected live Luft proof show
the required behavior, backend changes are reviewable and unpushed, findings
and decisions are recorded below, and the completion note names #5H9AKZ
without beginning it.

/Result./ Complete from exact experiment base
=21ba05d101e986ac6a0f60a512d62468c3eeca6a=, subsequently committed and
rebased onto =897bb5d05423d5aa77de466f5f77076eb678cf0a=.  The live proof and
focused tests established every required backend fact; see #WTODNV.  #5H9AKZ
is the next candidate gate, not work begun by this stage.

** DONE Build =luv/workbench= as the shell substrate
:PROPERTIES:
:ID: 5H9AKZ
:END:

/Intent./ Introduce one Luv-owned full-viewport workbench frame, layered pane
layout, shell-first event router, explicit passive/modeless/transient/modal
state, application input suspension, presentation composition, and lifecycle.

/Evidence./ This stage depends on #BY51K0 selecting proven resize, event, and
rendering seams.  Initially the shell may host compatibility content internally
if that keeps behavior stable while establishing one owner.

/Done when./ A small application protocol supplies identity, command frame,
semantic hooks, input suspension, and stop/lifecycle boundaries; shell tests
cover routing and teardown; no application-specific tool migration is hidden
inside this stage.

/Result./ Complete from exact base
=34a4c545454150e17ac773495b9c26c10a836cc8=.  =luv/workbench= owns one
textureless full-viewport frame, fixed semantic layers, shell-first routing,
input suspension, direct replay, atomic admission, quiescence, and release.
The attempted portable postlude was rejected by executable Vulkan evidence;
the smallest safe seam refreshes before presentation and replays in the
application's existing final pass.  No tool migrated.  #TEFU1T is the next
candidate gate.

** DONE Make Luft conform with status only
:PROPERTIES:
:ID: TEFU1T
:END:

/Intent./ Attach the shell once to Luft and render only the shared status pane
through it.  Keep the other current instruments temporarily so the first
application cutover remains observable and reversible.

/Evidence./ Status is passive, always visible, and the least demanding proof
of resize, semantic sampling, render order, startup, and teardown.

/Done when./ Luft supplies no status-specific frame, compositor, placement, or
event adapter; live resize and shutdown remain healthy; and the old status
instrument is deleted.

/Result./ Complete from exact =origin/main= and worktree base
=bfa9e503b5cf6f0bbc20f9fcf2e196e4dddf20d9=.  Luft startup now attaches one
workbench whose fixed passive layer owns the shared status pane; the former
status instrument, frame/compositor construction, affine placement, refresh,
encode, release, and opening adapter are deleted.  Focused, full, and live
evidence is recorded in #WTODNV.  Luvcraft retains its existing overlay
compatibility path.  The reviewed change was subsequently rebased onto exact
=origin/main= =17f47bb59249342474df830da8cb684c1f8b705f= and revalidated there.
#ZNLZQU is the next candidate gate and was not begun.

** TODO Migrate M-x and source update
:PROPERTIES:
:ID: ZNLZQU
:END:

/Intent./ Move the first transient/modal workflows into shell panes and one
focus policy.  Preserve source review's pinned fast-forward, live ASDF load,
cancellation, quiescence, and partial-assimilation reporting.

/Evidence./ These tools currently duplicate frame construction, coordinate
inversion, event forwarding, modality, and cleanup.  Together they prove both
synchronous command execution and asynchronous modal work.

/Done when./ Luft has no M-x or source-update instrument adapter, pointer and
keyboard use the shell's sheet geometry and focus model, and teardown cannot
deadlock against live-load canvas fences.

** TODO Migrate metabar and lobby
:PROPERTIES:
:ID: C9US2D
:END:

/Intent./ Move the transient control drawer and modeless detailed lobby into
their workbench layers while keeping the lobby radio's lifetime independent
of panel visibility.

/Evidence./ This stage exercises queued semantic operations, animation,
inside/outside pointer policy, modeless visibility, and a background service
with more than one view.

/Done when./ Neither component has a Luft-specific frame/compositor/input
adapter and the compact status summary remains independent of the detailed
lobby pane.

** TODO Move services and delete the instrument system
:PROPERTIES:
:ID: RYHYDG
:END:

/Intent./ Give Tracy, the application agent, and any remaining nonvisual owner
explicit service lifecycle coordinates; then remove the Luft weak registry,
numeric priority, generic refresh/encode/event protocol, and around-method
interception.

/Evidence./ Invisible instruments demonstrate that lifecycle ownership was
conflated with visual panes.  Preserve the stop-controller and frame-boundary
guarantees, not the registry shape.

/Done when./ =luft/render/instruments.lisp= and all instrument adapters are
deleted, the shell owns global panes, services have named owners, full tests
pass, and a running Luft application opens, uses, and closes every migrated
surface cleanly.

* Decision log
:PROPERTIES:
:ID: 3LH9F7
:END:

** 2026-08-28 — The workbench is substrate, not an extension registry

Status, lobby, metabar, M-x, and source review are known screen-space
affordances supplied by Luv.  Their usefulness comes from not requiring an
atelier to author a host integration for each one.  Do not hoist Luft's
instrument protocol or invent another arbitrary registration API.

** 2026-08-28 — Screen-space shell and application atelier remain distinct

The shell owns global panes, focus, modality, technical visual language, and
service views.  Luft owns its renderer, world, site inspector, authored tools,
commands, semantic field values, and resource release order.

** 2026-08-28 — Luft proves the protocol before Luvcraft migrates

Luft is small enough to expose the right application boundary.  Luvcraft's
spatial overlays and world focus are neither the design template nor immediate
cleanup scope.

** 2026-08-28 — Prefer one workbench frame and one presentation postlude

One layered frame lets McCLIM sheet geometry own rendering placement and input
hit testing.  One shell postlude in the portable presentation transaction is
more substrate-like than an application renderer callback.  Both remain
hypotheses until #BY51K0 proves backend behavior.

** 2026-08-28 — Embedded mirrors admit selected portable events explicitly

The application remains the canvas's sole native event handler and offers
selected portable events to =DISPATCH-EMBEDDED-MIRROR-EVENT=.  The mirror's
existing distributor then owns child hit testing, pressed-sheet behavior,
keyboard focus, and ordinary sheet delivery.  Do not install an embedded
mirror as the canvas handler and do not reproduce McCLIM hit testing in an
application adapter.

** 2026-08-28 — Logical resize is an event, not a parallel layout protocol

Forwarding =CANVAS-WINDOW-RESIZED-EVENT= through the same embedded entry emits
McCLIM's ordinary window-configuration event.  Top-level geometry change then
runs the layout protocol and moves/resizes overlapping children before the
retained repaint.  #5H9AKZ should use that operation rather than expose a
second mirror-resize API.

** 2026-08-28 — One textureless frame is the proven workbench render shape

One transparent full-viewport embedded mirror composes descendant media in
back-to-front sheet order, retains each child's logical clip, and replays the
result directly into the application's native-density destination pass.  The
proof mirror acquired no texture.  The backend therefore does not require a
pane raster, per-pane compositor, or renderer-owned workbench; a portable
postlude remains the preferred shell boundary.  Whether that postlude opens a
load-op pass or shares an application's final pass belongs to #5H9AKZ, not to
this backend proof.

** 2026-08-28 — The shell fallback shares the application's final pass

The preferred portable postlude was attempted against the concrete Vulkan
transaction.  Preserving the completed application attachment requires a
=:load= render pass, which the portable Vulkan validator rejects as
=:UNSUPPORTED-RENDER-PASS=.  Do not weaken that validator or copy the frame.
The production seam is therefore the smallest proven alternative: the
application refreshes the shell before opening its renderer pass and supplies
only its already-open final presentation pass and surface texture for retained
direct replay.  The shell still owns the frame, layer geometry, event policy,
semantic stream, compositor, and resources.

** 2026-08-28 — Interactive shell state suspends application input once

Passive state never takes focus or suspends application input.  Modeless,
transient, and modal state share one opaque application suspension token.
Opening the first interactive layer clears held application input, releases
relative capture through the application protocol, and records matching key
and button releases for shell-side suppression.  McCLIM focus and descendant
geometry decide modeless hits and transient outside dismissal; modal state
captures all keyboard and pointer input.  Closing the final interactive layer
returns the token without synthesizing pointer recapture.

** 2026-08-28 — Transparent roots are routing structure, never authored hits

McCLIM's concrete =BASIC-PORT= routing already computes the innermost pointer
target and publishes it as =POINTER-SHEET=.  Shell policy reads that result
after embedded dispatch and accepts only a proper descendant of the semantic
layer root.  The full-viewport root itself is transparent background: modeless
presses pass through, transient presses dismiss, and neither retains the root
as keyboard focus.  A neutral common layout sheet holds focus until an authored
descendant acquires it through ordinary McCLIM routing; per-layer authored
focus is remembered and restored across higher semantic layers.

Every shell-consumed key or button press also starts a shell-held cohort tied
to the consuming semantic layer.  Its release remains shell-owned while that
layer is visible and is swallowed without mirror delivery if the layer closed
while handling the press.  Thus an action may synchronously close the final
interactive layer without leaking an orphan release downstream.

** 2026-08-28 — Shell admission and teardown use existing lifecycle gates

One weak application-to-shell ownership map is not an extension registry.
Construction and publication cross the native frame boundary under the
application stop controller.  A losing constructor releases its private frame;
stop first quiesces shell work, then detaches and crosses a terminal owner
request before destroying the frame and direct compositor while the device is
live.  Luft's terminal order releases the shell before instruments, renderer,
canvas, and device.

** 2026-08-28 — Status is a fixed passive child of the workbench frame

The workbench frame carries the shared status state and constructs the status
pane inside its passive layer.  McCLIM sheet allocation fixes that pane at the
logical top edge and gives it the retained clip and painter position.  Generic
workbench refresh samples and prepares it; generic final-pass replay presents
it.  Luft supplies only its existing semantic methods and the one application
attachment at startup.  Do not add a status pane registry, compositor hook,
screen-state affine, or per-application open/close adapter.

** 2026-08-28 — Luvcraft status compatibility remains deliberately separate

The shared status component still supports its standalone embedded frame,
screen-state affine, and direct-widget overlay for Luvcraft.  Those mechanisms
are compatibility for the deferred Luvcraft migration, not dependencies of a
conforming Luft shell.  TEFU1T deletes only Luft's status adapter and does not
pull Luvcraft into the workbench substrate.

* Experiment notes
:PROPERTIES:
:ID: WTODNV
:END:

Record each experiment with date, execution thread, exact revision, question,
commands or live actions, observed result, interpretation, and resulting
decision.  Failed experiments belong here when they eliminate an architectural
path.  Do not paste complete test transcripts.

** 2026-08-28 — BY51K0 embedded backend proof

- /Execution thread and revision./ =T-01a04a50-c51f-76ec-850a-faf38ac4942e=,
  exact base =21ba05d101e986ac6a0f60a512d62468c3eeca6a=; experiment code and
  this record were first verified as uncommitted, unpushed worktree changes on
  that revision, then committed and rebased onto
  =897bb5d05423d5aa77de466f5f77076eb678cf0a= after review.
- /Question./ Can one transparent full-viewport embedded direct-GPU frame with
  two overlapping ordinary child panes use McCLIM itself for logical resize,
  child layout, clipping, painter order, hit testing, focus, and dispatch from
  portable canvas events while retaining native-density final-pass drawing?
- /Commands and live actions./ Ran =make=; =./sly parinfer --strict --check
  FILE= for each changed Lisp file; =./env --slim sbcl --noinform
  --disable-debugger --eval '(require :asdf)' --eval '(asdf:test-system
  :luv/mcclim/test)' --quit=; and =make test=.  Started the durable image with
  =./sly start=; loaded =luv/mcclim= and =luft/render=; opened Luft with
  =./sly play luft=; mounted
  =LUFT.RENDER::OPEN-VIEWER-WORKBENCH-BACKEND-PROOF=; captured
  =build/workbench-proof.png= and =build/workbench-proof-focused.png=; sent a
  portable left-button press/release at logical =(500, 300)= in the overlap
  followed by a portable =K= press; forwarded a synthetic logical resize from
  =1100x800= to =900x700=, inspected top-level and child geometry plus every
  prepared command clip, then restored =1100x800=.  Reloaded =luft/render=,
  unmounted the proof, stopped Luft, ran =scripts/wiki dangling= and
  =scripts/wiki build=, and ran =git diff --check=.  Finally entered the full
  cached environment with =LUV_DEV_SHELL=default ./env sh -c 'test -r
  "$LUV_GHOSTTY_LIBRARY"'= and ran =LUV_DEV_SHELL=default make test=.
- /Observed result./ The focused backend system reported 407 passed, zero
  failed, zero skipped.  Nix fetched =libghostty-vt= from the Luv binary cache,
  the configured shared library existed, and the full run passed all 23 suites
  in 9.1 seconds with 8 workers, including =luv/ghostty/test= and
  =luv/terminal/test=.  The live canvas remained healthy.  The transparent
  overlay left Luft visible; blue back and orange front panes overlapped, the
  front pane painted last, and deliberately oversized rectangle draws stopped
  at sharp child bounds.  Text was crisp and readable.  At =900x700= the top
  sheet became exactly =900x700=; back moved to =(72,70)= at =540x434= and
  front to =(324,210)= at =504x392=.
  Prepared commands were ordered back geometry/text before front geometry/text
  and carried clips =(72 70 612 504)= and =(324 210 828 602)= respectively.
  The mirror texture remained =NIL=.  The overlap click logged
  =(:FOCUS :FRONT)= then =(:POINTER :FRONT (104 60))=; the following key
  logged =(:KEY :FRONT :K)= and the port's keyboard focus was the front pane.
  The focused screenshot visibly showed “Front pane · keyboard focus” and
  “last event: KEY / FRONT / K”.  Wiki build and diff checks succeeded; wiki
  validation still reports the unrelated existing =TR4VLR= and =H1GHLD=
  mentions in =captures/luft-portraits.lisp=.
- /Interpretation./ Existing descendant stream composition, command-grain
  clips, and direct replay were sufficient.  The only backend seam missing
  from the desired boundary was a small public entry which forwards an event
  to an embedded mirror without giving it native canvas ownership.  McCLIM's
  existing configuration and distribution paths then supply layout, hit
  testing, and focus without application coordinate inversion.
- /Resulting decisions./ Keep one full-viewport textureless mirror; forward
  selected portable events through =DISPATCH-EMBEDDED-MIRROR-EVENT=; represent
  logical resize with the existing portable resize event; retain direct replay
  into the destination pass.  The evidence opens review of #5H9AKZ but does
  not select its final pass/postlude mechanics or begin shell construction.

** 2026-08-28 — 5H9AKZ production shell substrate

- /Execution thread and revision./ =T-01a04a7a-d5a8-7299-82d2-fef07db98d40=,
  exact =origin/main= and worktree base
  =34a4c545454150e17ac773495b9c26c10a836cc8=; all changes remained local and
  unpushed.
- /Questions./ What is the smallest application protocol which lets Luv own
  one shell, can the portable transaction safely append presentation after
  application encoding, and can routing, suspension, admission, quiescence,
  and terminal release remain executable without reproducing Luft's numeric
  instrument registry?
- /Commands and live actions./ Read #M9MI6Y and this worksheet in full and
  inspected the BY51K0 fixture, mirror dispatch, descendant composition,
  direct compositor, portable/SDL presentation, stop controller, Luft input,
  instrument, render, and teardown paths.  Ran strict Parinfer on every changed
  Lisp file; focused =luv/workbench= and =luft/render= tests; and a live durable
  Luft session.  In the live session attached the empty generic shell, inspected
  mirror texture and event ownership, attempted the portable =:load= postlude,
  retained its exact failure, replaced it with final-pass replay, resumed the
  canvas, attached again, exercised modal suspension/clear-held-input, sent a
  synthetic =900x700= resize through the shell router, captured
  =build/workbench-shell.png=, detached, and inspected restored viewer event
  ownership and canvas health.  Finally ran
  =LUV_DEV_SHELL=default ./env sh -c 'test -r "$LUV_GHOSTTY_LIBRARY" && make
  test'=, =scripts/wiki dangling=, =scripts/wiki build=, and
  =git diff --check=.
- /Observed result./ The first live postlude frame parked with
  =:UNSUPPORTED-RENDER-PASS= at Vulkan =BEGIN-RENDER-PASS=; the window remained
  pumped and resize dispatch remained live.  With the fallback, the empty shell
  retained =NIL= mirror texture, became the sole outer event handler, replayed
  in Luft's existing final pass, and ran with a healthy canvas.  Modal opening
  changed a held-control count from one to zero, suspension was true only while
  open, relative capture was false, and close resumed the token.  Synthetic
  resize made the top sheet exactly =(0 0 900 700)=.  Detach removed shell
  publication and restored the =VIEWER= handler.  The full environment found
  the Ghostty library readable and passed all 24 suites (28 parallel tasks) in
  9.2 seconds, including =luv/ghostty/test=, =luv/terminal/test=, the new
  =luv/workbench/test=, and both =luft/render/test= tasks.  Strict Parinfer,
  wiki build, and diff checks passed.  Wiki dangling retained only the existing
  unrelated =TR4VLR= and =H1GHLD= code mentions.
- /Interpretation./ The portable postlude preference is not safe in the
  current HAL, but no parallel compositor is necessary.  One application call
  before presentation and one direct replay call in its final pass preserve
  every textureless backend invariant with less machinery than broadening the
  render-pass contract.
- /Resulting decisions./ Keep the fixed passive/modeless/transient/modal layer
  owner and shell-first wrapper; use opaque input suspension with release
  suppression; use stop-controller publication, explicit quiescence, terminal
  fence, and device-live release; use final-pass replay; migrate no tool here.
  Evidence opens #TEFU1T (Luft status only) and nothing later.

** 2026-08-28 — 5H9AKZ routing review corrections

- /Execution thread and revision./ Same =5H9AKZ= thread and exact base
  =34a4c545454150e17ac773495b9c26c10a836cc8=; changes remained uncommitted and
  unpushed.
- /Questions./ Does McCLIM route empty full-screen layer background to the
  layer root, and can a shell-consumed press leak its release when its handler
  closes the final interactive layer?
- /Commands and live actions./ Read McCLIM's concrete =BASIC-PORT=
  =DISTRIBUTE-EVENT=, =SYNTHESIZE-BOUNDARY-EVENTS=, and
  =COMPUTE-POINTER-EVENT-SHEET= paths.  Added focused strict-descendant and
  synchronous-close/release tests.  In fresh durable image =LAV976= attached
  the empty Luft shell, sent real embedded pointer presses at =(20,20)= through
  modeless and transient states, then sent a modal Escape press, closed the
  layer, and sent its release.  Ran focused =luv/workbench= and =luft/render=
  tests, strict Parinfer, and the full Ghostty-enabled =make test= command.
- /Observed result./ Before correction, =SHOW-WORKBENCH-LAYER= focused the
  full-viewport root and inclusive ancestry treated that root as inside; no
  state tracked shell-consumed presses.  Corrected live modeless routing
  reported =:POINTER-TARGET-IS-ROOT T= but =:CONSUMED NIL= and
  =:FOCUS-IS-NEUTRAL T=.  Corrected transient routing reported =:CONSUMED T=,
  =:TRANSIENT-VISIBLE NIL=, and neutral focus.  Modal Escape reported one
  shell-held press, then =:RELEASE-CONSUMED T= and zero held entries after
  closure; the canvas remained healthy at 181 rendered frames.  Focused suites
  passed 27 =luv/workbench= checks and 274 =luft/render= checks.  The full run
  found Ghostty readable and passed 24 suites / 28 tasks in 9.6 seconds,
  including Ghostty, terminal, workbench, and both Luft render tasks.
- /Interpretation./ Both review concerns were real.  Reading McCLIM's routed
  pointer target avoids a second coordinate/hit-test implementation.  Pairing
  consumed presses with releases in the substrate closes the race even when
  authored handling changes layer state synchronously.
- /Resulting decisions./ Layer roots are transparent routing/layout only;
  only proper authored descendants count as modeless/transient hits.  Keep
  authored focus per layer and use a neutral common sheet otherwise.  Track
  shell-consumed press/release cohorts centrally.  No tool or application
  protocol was added; #TEFU1T remains unopened work.

** 2026-08-28 — TEFU1T Luft passive-status cutover

- /Execution thread and revision./ =T-01a04aab-45e1-76ef-9ec3-9841ffc65082=,
  exact =origin/main= and worktree base
  =bfa9e503b5cf6f0bbc20f9fcf2e196e4dddf20d9=; all changes remained local and
  unpushed.
- /Questions./ Can the shared status frame also be the one workbench frame so
  that the fixed passive McCLIM layer owns layout, clipping, painter order,
  retained media, and hit transparency?  Can Luft then delete every status
  instrument coordinate while preserving throttled semantics, resize,
  native-density direct replay, shell-first event ownership, one startup
  attachment, and device-live release?  Can Luvcraft remain on its current
  overlay compatibility path unchanged?
- /Commands and live actions./ Read #M9MI6Y and this worksheet in full and
  inspected the shared status implementation/tests, workbench substrate/tests,
  Luft instrument/start/render/input/stop paths, and Luvcraft status adapter
  and tests.  Ran =LUV_DEV_SHELL=default make=; strict Parinfer on every
  changed Lisp file; full-environment focused =luv/mcclim=, =luv/workbench=,
  =luft/render=, =luvcraft/mcclim=, and =luvcraft= tests after confirming
  =$LUV_GHOSTTY_LIBRARY= readable; and =LUV_DEV_SHELL=default make test=.
  Started a fresh durable image and used ordinary =./sly play luft=, not a
  proof function.  Inspected the shell, passive/status sheet geometry, mirror,
  retained command types, semantic fields, input state, and instrument list;
  captured the default =1100x800= frame; resized the native canvas to =900x700=
  and captured it again; forwarded a pointer event while modeling established
  relative capture; validated direct presentation and idempotent shell start;
  restored =1100x800=; stopped through =./sly stop-playing=; and inspected
  publication registries and retained failures.  Also ran =scripts/wiki
  dangling=, =scripts/wiki build=, and =git diff --check=.
- /Observed result./ The focused suites passed 407 McCLIM, 35 workbench, 353
  Luft render, 139 Luvcraft/McCLIM, and 116 Luvcraft checks, with all failures
  and skips zero.  The Ghostty-enabled full run passed all 24 suites / 27 tasks
  in 12.1 seconds with 8 workers, including Ghostty, terminal, workbench, both
  Luft render tasks, McCLIM, and Luvcraft compatibility.  The first live
  construction exposed two discarded pane-construction shapes: referring to a
  sibling pane lexically signalled unbound =BAR=, while declaring an unplaced
  sibling left no pane to adopt.  Constructing the known status child directly
  inside the passive pane fixed both without an application registry or
  adapter.  The subsequent default startup was healthy.  Exactly one shell
  was published as the canvas outer handler with the viewer downstream; its
  passive layer was visible while active interactive layer and suspension were
  =NIL=.  The shell mirror texture was =NIL= and retained only analytic and
  prepared-text commands.  At =1100x800= the status region was exactly
  =(0 0 1100 28)= and showed application, PID, FPS, heap, lobby, worktree,
  bevel, view, and mode.  At =900x700= the top sheet became
  =(0 0 900 700)=, status became =(0 0 900 28)=, logical text size remained
  12, and the inspected image stayed crisp and top-aligned.  A pointer event
  reached Luft and updated its position to =(123 234)= while modeled capture
  stayed true, with no active shell layer or suspension.  The orb's SDL driver
  rejected actual relative mode as unsupported, so native capture itself could
  not be enabled there.  Direct-presentation validation succeeded, a second
  =START-WORKBENCH= returned the same shell, and the only Luft instrument was
  the deferred lobby adapter.  Clean stop left zero workbench publications,
  zero instrument owners, no viewer, and no retained canvas failures.  Wiki
  build and diff checks succeeded; dangling retained only the existing
  unrelated =TR4VLR= and =H1GHLD= code mentions.
- /Interpretation./ Frame inheritance preserves one shared semantic status
  implementation while making the workbench the only Luft status frame and
  compositor.  Fixed descendant construction and McCLIM allocation are enough
  for top-edge layout, clipping, painter order, and resize; passive routing
  forwards input without touching suspension or capture.  The existing generic
  refresh, final-pass replay, admission, event wrapper, fence, and release path
  need no status-specific hook.
- /Resulting decisions./ Keep Luft's semantic status methods and delete its
  status instrument class plus priority, construction, lookup, refresh, encode,
  affine placement, release, and opening methods.  Attach one workbench after
  lobby infrastructure during default startup and release it before instruments,
  renderer, canvas, and device.  Preserve Luvcraft's current standalone status
  overlay unchanged.  Evidence opens review of #ZNLZQU and nothing later.
- /Reviewed rebase./ Parent review accepted the ownership and deletion cut.
  Commit =04856a4= rebased without textual conflict onto exact =origin/main=
  =17f47bb59249342474df830da8cb684c1f8b705f=.  Upstream's authored world
  export, startup demand clock, and 123 new world tests remain intact;
  the overlap resolution only removes the old status exports/adapter, adds the
  semantic-only status test, and attaches/releases the workbench.  Strict
  Parinfer remained clean.  Focused McCLIM, workbench, Luft render,
  Luvcraft/McCLIM, and Luvcraft suites passed 407, 35, 370, 139, and 116 checks
  respectively.  A readable-Ghostty full run passed all 24 suites / 27 tasks.
  Wiki validation/build and final Git checks also passed.  The rebased stage
  remains one local commit ahead of =origin/main= and unpushed.

* Completed prose updates
:PROPERTIES:
:ID: QF07KG
:END:

** 2026-08-28 — Architecture and worksheet separated

- #M9MI6Y records why the Luv workbench is a transcendental substrate and why
  the current application-by-tool adapter matrix is not the desired boundary.
- This worksheet records invariants, current evidence, open questions, ordered
  stage gates, decisions, experiments, and the exact next action.
- The earlier extraction account #7OH71A remains historical evidence: shared
  mechanisms were successfully moved below both games, but the adapters it
  accepted are now the migration source rather than the destination.

** 2026-08-28 — Embedded workbench backend proved

- #BY51K0 established a transparent full-viewport embedded frame with two
  overlapping child panes in a live Luft destination.
- Ordinary McCLIM resize/layout, retained clips and painter order, sheet hit
  testing, keyboard focus, and selected portable canvas-event dispatch all
  worked through one textureless direct-GPU mirror.
- Focused backend tests passed 407 checks; inspected live geometry, retained
  commands, event logs, focus state, and screenshots agreed with the claims.
- No production shell or tool migration was started.  #5H9AKZ is the next
  candidate gate for architectural review.

** 2026-08-28 — Luv workbench shell substrate completed

- =luv/workbench= now owns one transparent full-viewport McCLIM frame, four
  fixed semantic layers, shell-first event routing and focus, application-input
  suspension, retained direct replay, and coordinated lifecycle.
- Focused checks cover semantic state/routing, transient dismissal, held-release
  suppression, atomic stop admission, and quiesce/fence/release order.  A live
  empty Luft attachment proved generic ownership without migrating status or
  any other instrument.
- Concrete Vulkan evidence rejected a separate portable =:load= postlude.  Luft
  therefore supplies only its existing final pass to the shell; no renderer,
  drawable acquisition, submission, texture, or arbitrary pane registry moved
  into Luv.
- #TEFU1T, Luft status only, is the next candidate.  It was not begun.

** 2026-08-28 — Luft status conformance completed

- Default Luft startup now attaches exactly one Luv workbench; its fixed
  passive layer owns the shared status pane's geometry, clip, retained stream,
  painter order, direct compositor, event transparency, and release.
- Luft retains only semantic application name, lobby, channel, and value
  methods.  Its old status instrument and every status-specific attachment,
  placement, refresh, encode, and lifecycle coordinate are gone.
- Live default startup, native resize, direct retained presentation, passive
  input forwarding, modeled capture preservation, idempotent attachment, and
  clean shutdown agreed with focused and full Ghostty-enabled checks.
- Luvcraft remains green through its existing direct-widget overlay.  #ZNLZQU,
  M-x plus source update, is the next candidate and was not begun.

* Exact next action
:PROPERTIES:
:ID: 53WN6F
:END:

Review the local #TEFU1T passive-status cutover and its default-startup,
resize, input, direct-presentation, compatibility, and teardown evidence.  If
accepted, #ZNLZQU, “Migrate M-x and source update,” is the exact next candidate.
Do not begin metabar, lobby, service migration, Luvcraft migration, or general
instrument-system deletion with that next cut.
