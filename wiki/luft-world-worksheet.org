#+title: A worksheet for the large Luft world
#+startup: overview

* Intent and non-goals
:PROPERTIES:
:ID: 59ZKO4
:END:

This is the living worksheet for turning Luft's small mountain sanctuary into
a genuinely large, demand-streamed voxel world with authored geography,
architecture, material variety, and regional voxel light.  The durable design
records in [[file:luft.org][the Luft notebook]], [[file:domains-and-bundles.org][Domains, bundles, and materialized worlds]],
[[file:frontier-traversals.org][Frontier traversals]], and [[file:block-world.org][the Luvcraft block-world notebook]] explain the
representations and prior experiments.  This page sequences the new work,
records what each experiment teaches, and names exactly one next action.
Execution threads are replaceable working sessions; they read and update this
worksheet rather than becoming the only place where a decision survives.

The intended world should feel composed before it feels generated: a river and
watershed, roads and passes, long sightlines, distinct regions, a substantial
settlement or fortress, and sparse bridges, ruins, quarries, caves, shrines,
and waystations make travel legible while still allowing wilderness, wrong
turns, and getting lost.  Deterministic procedural fields supply local terrain
and detail under that authored plan.  Walking must cause source materialization,
light solving, meshing, publication, and eviction rather than selecting meshes
from a world already held in memory.

This work does /not/ restore the mesher deleted by commits =366ffe4= and
=ae99b9d=, reintroduce mixed bevel widths or material-dependent topology,
design transparent terrain, begin with level of detail or vertical streaming,
or extract a universal voxel engine before Luft provides a second concrete
client for Luvcraft's best substrate ideas.

* Established invariants
:PROPERTIES:
:ID: 94AJDM
:END:

- Luft retains one width-one 256-star atlas.  Materials and light do not alter
  star selection, triangle ownership, bevel geometry, or the four-word active
  site stream.
- Canonical authored content names a material at each cell; occupancy is the
  projection =material != air=.  Geometry, light, meshes, and GPU resources are
  derived products with independently checkable provenance.
- World description, resident chunks, and rendered chunks are different
  things.  The source plus sparse authored overlay can rematerialize an evicted
  chunk; resident storage is not the save format.
- An absent neighbor is explicitly open sky, closed world, or unknown because
  it is nonresident.  Streaming state never silently becomes authored air,
  darkness, or open sky.
- The canvas/render owner remains the single writer of residency and GPU
  state.  Workers receive immutable captures; incarnation and dependency
  stamps make obsolete generation, light, and appearance results harmless.
- Geometry and appearance are separate products even if an early worker
  computes both.  Repainting or relighting may not change geometry bytes or
  triangle counts.
- Sun and shadow maps retain direct-light ownership.  Voxel sky light scales
  ambient illumination; RGB4 block light contributes local radiance.
- The first proof is opaque, uses one small global material vocabulary, and
  accepts dense full-height chunk lanes until measurement earns compression.
- Geography has authored macrostructure.  Noise may roughen a place but may
  not be the only reason one place differs from another.

* Current architecture and evidence
:PROPERTIES:
:ID: AMY4CI
:END:

The simplified implementation is the foundation, while deleted code is design
evidence rather than a restoration source.

- [[file:../luft/mesh.lisp][luft/mesh.lisp]] defines one =surface-mesh= as four unsigned words per
  active star.  =+MESH-BEVEL-WIDTH+= is one and the renderer metadata does not
  participate in geometry.
- [[file:../luft/mesh-query.lisp][luft/mesh-query.lisp]] selects active lattice stars from dense 64x64
  occupancy fibers and their one-cell neighborhood.
- [[file:../luft/star-table.lisp][luft/star-table.lisp]] and [[file:../luft/atlas.lisp][luft/atlas.lisp]] own the fixed geometry and
  its symmetry-derived 256 entries.  This is the place where a static
  triangle-to-incident-cell appearance selector might be proved.
- [[file:../luft/render/render.lisp][luft/render/render.lisp]] flattens only the active-site words into the
  terrain population.  Its current =streaming-scene= eagerly partitions a
  complete finite scene into an in-memory store, so only meshing and GPU
  residency are streamed.
- [[file:../luft/render/shaders.lisp][luft/render/shaders.lisp]] gives terrain fixed top, side, and bottom tones
  under ambient sky/ground, direct sun, shadow-map visibility, and fog.  It
  receives neither terrain material identity nor voxel light.
- [[file:../luft/render/materials.lisp][luft/render/materials.lisp]] and [[file:../luft/voxel-light.lisp][luft/voxel-light.lisp]] retain useful semantic
  material and RGB4 finite-solver evidence.  Current terrain does not consume
  those fields; remaining use by scene and torch paths must not be mistaken
  for a scalable regional design.
- [[file:../luft/render/render.lisp][=MAKE-HIGHLAND-SANCTUARY-SCENE=]] already sketches a 512x512 landscape
  with three sparse landmarks, but eagerly builds all sixty-four chunks and
  derives most macrostructure from value noise.  It is a content experiment,
  not the demand-world architecture.
- [[file:../domains/domains.lisp][domains/domains.lisp]] and
  [[file:../arithmetic/records/columnar.lisp][arithmetic/records/columnar.lisp]] already provide neutral finite domains,
  vocabularies, and typed columnar materializations.
- [[file:../luvcraft/world.lisp][luvcraft/world.lisp]], [[file:../luvcraft/terrain.lisp][luvcraft/terrain.lisp]], and
  [[file:../luvcraft/streaming.lisp][luvcraft/streaming.lisp]] demonstrate palette-coded chunk content,
  deterministic source materialization, sparse overlays, demand tokens,
  incarnation validation, and bounded eviction.  Their 16-cubed block-world
  classes and gameplay contracts remain Luvcraft-specific.
- [[file:../luvcraft/light.lisp][luvcraft/light.lisp]] and [[file:../luvcraft/frontier-light.lisp][luvcraft/frontier-light.lisp]] demonstrate separately
  revised columnar light fields, palette-projected propagation tables,
  explicit absent-boundary semantics, coherent regional publication, and
  compiled frontier propagation.

* Open technical questions
:PROPERTIES:
:ID: M6MATV
:END:

1. /Answered by #33UIQ6: no./  Star =#x81='s positive junction triangle is
   fixed by a proper three-axis cycle that moves every empty sample.  A
   symmetry-stable sample /set/ plus a commutative appearance policy must
   replace the proposed distinguished occupied/empty selectors.
2. Is one appearance record per active star the right first ABI, containing
   the eight neighboring material codes and light values while leaving the
   active-site stream byte-identical?  Measure alignment and upload cost before
   compacting it.
3. Should the first palette be one world-wide append-only =u8= vocabulary, or
   do authored generation and editing already demonstrate a need for wider or
   chunk-local codes?  Prefer the world vocabulary until evidence says no.
4. What is the smallest immutable Luft resident-cell-chunk value that keeps
   material content canonical while making occupancy fibers and chains cheap
   derived views?
5. Can a visible radius plus one complete guard chunk settle all 15-level sky
   and RGB4 block light, including diagonal star appearance dependencies, or
   does publication require a differently shaped capture?
6. Which part of =luvcraft.world.fields= is sufficiently experiment-neutral to
   move below both games now?  Frontier program semantics appear general, but
   extraction of their execution lowering waits until Luft is a real second
   client.
7. How should authored roads, rivers, region masks, and structures compose at
   chunk boundaries without requiring one giant scene builder or making local
   noise the world plan?

* Ordered stages
:PROPERTIES:
:ID: N3DRNE
:END:

The stages are gates, not a backlog to execute speculatively.  An execution
thread owns one stage, reads this whole worksheet and the named design records,
records experiments and decisions here, leaves reviewable unpushed changes,
writes a prose completion update, and identifies rather than begins the next
stage.  The architectural parent thread reviews the evidence before admitting
the next gate.

** DONE Prove static atlas appearance selectors
:PROPERTIES:
:ID: 33UIQ6
:END:

/Intent./ Determine whether every triangle in all 256 immutable star entries
can have a purely static selector for the incident occupied cell that supplies
material and the outward empty cell that supplies voxel illumination.  Keep
generated geometry and the active-site ABI unchanged.

/Evidence./ The new atlas already derives all geometry from eight-cell stars
and symmetry transforms.  A selector stored in unused atlas metadata would let
the shader interpret parallel cell fields without rebuilding faces, stocks,
material junction policy, or mixed bevel machinery.  This attractive design
must be proved over the complete atlas rather than assumed from ordinary faces.

/Result./ Complete-atlas executable evidence disproves the single-selector
hypothesis.  Forty-eight of 4,200 emitted triangle primitives have a cubical
stabilizer with no fixed occupied or empty sample of the required kind.  In
the smallest counterexample, star =#x81='s positive junction triangle is fixed
by a proper 120-degree axis cycle while all six empty samples move.  No
selector metadata was added.  Tests preserve the 4,200-triangle census and the
exact four-word one-cell active-site stream.  The next stage is #FTZ8FD, which
must use symmetry-stable sample sets and a commutative junction policy rather
than pretending that one distinguished voxel exists.

** DONE Add the palette-coded terrain appearance product
:PROPERTIES:
:ID: FTZ8FD
:END:

/Intent./ Represent basic opaque materials as semantic vocabulary entries and
dense cell codes, compile one parallel appearance record per active star, and
shade grass, soil, exposed stone, and limestone through one small descriptor
table and the unchanged atlas draw path.

/Evidence./ Existing finite scenes provide enough authored material variation
to prove the appearance ABI before demand generation changes residency.
Material-only changes should rebuild and upload appearance while preserving
geometry exactly.

/Done when./ The sanctuary visibly distinguishes basic materials under current
sun and shadows; faces, bands, and junctions use symmetry-stable sample sets
with one explicit commutative material/light reduction policy where needed;
tests prove that policy equivariant over all 48 cubical transformations and
show repainting changes no geometry bytes or triangle counts; the shadow pass
does not bind appearance; and the completion note names #H1ASQU.

/Result./ Complete at implementation revision
=2948176327086d6177b4ac6109ec6585078a6493=.  Every active star now has an
eight-u8 appearance sidecar parallel to the unchanged =(X Y Z STAR)= stream.
Ordinary faces read their unique occupied sample; bands read all occupied
cells incident to their half-edge; junctions read all occupied star cells.
Material tones use an equal-weight arithmetic mean and the paired empty-cell
light policy is componentwise maximum.  Exhaustive tests prove the sample sets
and both componentwise reductions equivariant under all 48 cubical
transformations.  The scene shader consumes a compact u8 palette and RGB8
top/side/bottom descriptor table; the shadow shader and bind group remain
geometry-only.  The finite sanctuary renders grass-capped soil, simple
cardinally exposed highland rock, and limestone architecture under its existing
sun and shadows.  Material repainting changes only appearance bytes and keeps
the complete geometry stream and triangle count exact.  #H1ASQU is next.

** TODO Materialize a demand-streamed authored world
:PROPERTIES:
:ID: H1ASQU
:END:

/Intent./ Replace the eager whole-scene store for ordinary play with one
deterministic Luft source, immutable resident chunks, sparse authored overlay,
camera demand, worker production, incarnation validation, and bounded
eviction.  Compose an authored river, road, pass, and one masonry destination
across chunk seams before adding broad procedural detail.

/Evidence./ Luvcraft proves the ownership pattern, but Luft's full-height
64x64 chunks, packed chains, star neighborhoods, and semantic architecture
remain distinct.  A finite-scene adapter should preserve atelier fixtures
without making them define the new source protocol.

/Done when./ Walking across a boundary causes observable source generation,
materialization, meshing, publication, and opposite-edge eviction; resident
CPU and GPU state remain bounded; stale worker results are rejected; the
authored route and destination survive chunk-independent generation; and the
completion note names #JDB26Y.

** TODO Publish stable regional sky and RGB4 light
:PROPERTIES:
:ID: JDB26Y
:END:

/Intent./ Add separately revised sky and block-light fields, palette-projected
opacity/emission tables, explicit open/closed/unknown boundary semantics,
immutable regional captures, frontier propagation, and appearance-only
publication.

/Evidence./ The current Luft finite RGB4 solver is a differential oracle;
Luvcraft supplies proven field, frontier, boundary, and coherent-publication
patterns.  The first implementation may specialize their laws to Luft rather
than generalize the executor prematurely.

/Done when./ Open ground, a roofed room, a skylight, and an RGB emitter render
correctly; one source and receiver cross a chunk seam; entering visible chunks
wait for stable guard data; relighting preserves geometry; differential tests
agree with the finite oracle; and the completion note names #DLH49J.

** TODO Make the first journey worth taking
:PROPERTIES:
:ID: DLH49J
:END:

/Intent./ Extend the proved source into a coherent first region with connected
and branching routes, named destinations, wilderness between them, durable
edits, and a resumable player position.  Tune generation and residency from
measured walking rather than from fixture construction time.

/Evidence./ Architectural composition is the product requirement that keeps a
large generated domain from becoming an oscillating height-map no-place.
Persistence should save source description, semantic edits, and resume state,
not resident chunks, light, meshes, or GPU products.

/Done when./ A player can leave the initial fortress, follow and lose a road,
cross at least one distant authored structure, return by another route, edit a
cell, restart, and find both edit and position preserved while residency stays
bounded.  The worksheet then decides the next measured need rather than
assuming transparency, LoD, vertical streaming, or simulation.

* Decision log
:PROPERTIES:
:ID: 3UTRKA
:END:

** 2026-08-28 — Reduce symmetry-stable appearance sets commutatively

An ordinary face uses its unique occupied member of the solid/air pair.  A
band uses the occupied subset of the four cells incident to its lattice
half-edge, and a junction uses the occupied subset of all eight star cells.
Terrain material tone is the equal-weight arithmetic mean over that set.  The
paired illumination rule for the corresponding empty set is componentwise
maximum.  Set images commute with every signed-axis permutation, arithmetic
mean is permutation-invariant, and componentwise maximum is associative,
commutative, and idempotent; exhaustive execution checks each emitted part
under all 48 cubical transformations.

Keep the eight semantic u8 material codes in a parallel active-star sidecar.
Pack only at the GPU upload boundary, and place the two sample masks in static
atlas metadata.  Do not select a canonical sample, move material into geometry,
or bind appearance in the shadow pass.

** 2026-08-28 — Reject distinguished triangle selectors at symmetric junctions

A single occupied-material and outward-empty-light sample per atlas triangle
cannot respect cubical symmetry.  Star =#x81='s positive junction triangle is
fixed by a proper three-axis cycle, but that cycle fixes none of its six empty
samples; the complete 256-star enumeration finds 48 obstructed emitted
triangles under the full 48-element cubical group.  Do not encode arbitrary
canonical-frame choices or checkerboard exceptions as appearance truth.

Keep all eight neighboring cell values in the proposed active-star appearance
record.  Atlas appearance metadata may name symmetry-stable sample /sets/;
#FTZ8FD must choose and prove a commutative reduction policy for tied junction
orbits.  This preserves geometry and lets ordinary faces remain direct without
claiming a geometrically distinguished voxel where none exists.

** 2026-08-28 — Preserve geometry and add a parallel appearance product

The simplified four-word star stream and fixed one-width atlas remain the
entire terrain geometry contract.  Material identity and voxel light travel in
a one-for-one active-star sidecar interpreted by static atlas sample sets and
symmetry-preserving reductions.  Do not restore face stocks,
material-dependent topology, mixed widths, repair passes, or one draw/atlas
per material.

** 2026-08-28 — Reuse substrate laws, not Luvcraft world classes

Finite domains, vocabularies, columnar materializations, immutable worker
captures, demand tokens, sparse overlays, derived-field revisions, explicit
boundary semantics, and coherent publication are valuable below either game.
Luvcraft's coordinate axes, 16-cubed storage, =block-kind= semantics, and
gameplay mutation contracts are not Luft's model.  Luft becomes the second
client whose concrete needs can justify later frontier-executor extraction.

** 2026-08-28 — Authored macrostructure precedes procedural detail

The large world begins with routes, watersheds, regions, sightlines, and sparse
destinations expressed in absolute world coordinates.  Deterministic noise and
local rules fill and roughen that composition; they do not substitute for it.

** 2026-08-28 — Stable visible light uses known guard content

Nonresidency remains unknown.  The playable path retains or withholds a visible
product until the content required to settle its bounded sky and RGB4 light is
known.  Provisional fields may aid inspection but are not presented as final
geography.

* Experiment notes
:PROPERTIES:
:ID: FQ5007
:END:

Record each experiment with date, execution thread, exact revision, question,
commands or live actions, observed result, interpretation, and resulting
decision.  Failed experiments belong here when they eliminate an architectural
path.  Do not paste complete test transcripts.

** 2026-08-28 — Palette-coded active-star appearance and sanctuary proof

- /Thread./ =T-01a04a69-0d4d-713e-a458-e344d25880f8=.
- /Revision./ Implementation commit
  =2948176327086d6177b4ac6109ec6585078a6493= on branch
  =stage/luft-terrain-appearance=, based exactly on reviewed =origin/main=
  revision =897bb5d05423d5aa77de466f5f77076eb678cf0a=.
- /Question./ Can the fixed width-one atlas shade the existing finite
  sanctuary's terrain, exposed stone, and limestone without changing geometry
  or inventing an asymmetric material selector at bands and junctions?
- /Commands and live actions./ Ran =make=; strict Parinfer checks on every
  changed Lisp file; =asdf:test-system "luft/test"=;
  =asdf:test-system "luft/render/test"=; and =make test=.  The renderer test
  compiled the terrain mesh, fragment, and geometry-only shadow shaders through
  both =luv.msl:compile-msl= and =luv.spir-v:compile-shader-specification=.
  Started the durable image with =./sly start=, opened Luft with
  =./sly play luft=, checked =./sly status= and =./sly failures=, captured
  =./sly screenshot build/ftz8fd-sanctuary.png=, and inspected the 1100x800
  BGRA8 frame directly.
- /Observation./ The complete model proof reports 608,971 checks and preserves
  the 4,200-triangle atlas census.  Focused renderer tests pass 347 claims.
  Repainting a two-material fixture replaces its eight-u8 sidecars while the
  =(X Y Z STAR)= bytes and triangle count remain equal; its emitted mixed
  sample set exercises a band or junction.  The main shader binds sidecar and
  descriptor buffers, while the shadow shader exposes only site, atlas, and
  camera resources.  The live Vulkan canvas remained healthy with no retained
  failures.  The inspected frame clearly separates green living-earth caps,
  narrow brown soil edges, dark exposed cliff rock, and pale limestone; sun
  shadows, beveled bands, and terrain/architecture junctions remain coherent.
- /Interpretation./ One dense eight-byte record per active star is sufficient
  for the first opaque proof.  Static sample sets plus commutative reduction
  solve the #33UIQ6 obstruction without a material mesher, face stocks, or an
  arbitrary canonical frame.  A tiny scene palette is enough to restore place
  identity while keeping geometry and shadow ownership unchanged.
- /Decision./ Keep this appearance ABI and policy for #H1ASQU.  Defer voxel
  light population of the already-defined empty-cell reduction, palette
  compression, and all demand-world work to their ordered stages.

** 2026-08-28 — Complete-atlas selector stabilizer proof

- /Thread./ =T-01a04a5e-dd2c-73dd-959d-1f8204688a14=
- /Revision./ =771c7dbee1577f4971f56663db99ba8a9696c831=, exactly the
  synchronized =origin/main= base requested for #33UIQ6, on dedicated branch
  =stage/luft-atlas-appearance-selectors=.
- /Question./ Can every one of the 4,200 emitted triangles across all 256
  width-one stars select one occupied material sample and one outward empty
  illumination sample equivariantly under cubical symmetry?
- /Commands./ Loaded =luft= in slim SBCL and enumerated all atlas triangles,
  all 24 proper rotations, and then all 48 signed-axis transformations; ran
  =./sly parinfer --strict --check luft/tests.lisp=,
  =./env --slim sbcl --non-interactive --eval '(require :asdf)' --eval
  '(asdf:test-system "luft/test")'=, and =make test=.
- /Observation./ A selector must be fixed by the stabilizer of its star and
  geometric triangle.  Full-group enumeration finds 48 triangle primitives
  whose stabilizer has no fixed sample of at least one required occupancy (24
  already fail under proper rotations).  For star =#x81= and triangle
  =((0 1 1) (1 1 0) (1 0 1))=, cyclic axis permutation fixes the star and
  triangle but moves every empty sample.  This rules out every empty selector,
  and therefore every stricter outward-empty selector.  The exhaustive test
  retains 4,200 triangles; the one-cell mesh retains its exact 32 words and 44
  triangles.  No production atlas, renderer resource, or shader changed.
- /Interpretation./ This is a group stabilizer obstruction, not a missing tie
  breaker.  Selecting by canonical orientation would hide an arbitrary frame
  choice and make appearance rotate or reflect inconsistently.
- /Decision./ Add no single-selector metadata and no special case.  Preserve
  all eight appearance inputs and let #FTZ8FD prove symmetry-stable sample sets
  plus an explicit commutative reduction policy before rendering materials or
  voxel light.

** 2026-08-28 — Architecture survey and Oracle consultation

- /Thread./ =T-01a04a2e-fab5-7206-90bd-94d965339cb9=
- /Revision./ =a509168= after fast-forwarding the clean checkout to
  =origin/main=; the initial survey was made at =efc94b1=, whose relevant Luft
  and Luvcraft files are unchanged by that fast-forward.
- /Question./ How can basic materials, voxel light, and actual demand streaming
  return without rebuilding the system removed by =366ffe4= and =ae99b9d=, and
  which Luvcraft concepts are genuinely substrate?
- /Actions./ Read the current star selector, atlas, terrain shader, scene
  streaming, material and voxel-light paths; read Luvcraft domains, palette
  content, production, light regions, and frontier programs; inspect the two
  deletion commits; ask the Oracle for an independent architecture and staged
  proof.
- /Observation./ Current geometry is one fixed atlas and one four-word record
  per active star.  Terrain has only orientation tones plus sun, shadow, and
  ambient light.  Scene material/light semantics partly survive but are
  globally coupled.  Luvcraft already demonstrates the desired ownership laws
  over a different chunk representation.
- /Interpretation./ Material and light belong beside geometry as fields and
  derived appearance, not inside topology.  Static triangle selectors are the
  smallest plausible bridge, but their complete-atlas validity is the first
  unresolved fact and therefore the first gate.
- /Decision./ Adopt the invariants and stage order on this worksheet.  Begin no
  production world or shader changes before #33UIQ6 is reviewed.

* Completed prose updates
:PROPERTIES:
:ID: 3OE183
:END:

** 2026-08-28 — The sanctuary regained terrain identity without material geometry

- #FTZ8FD adds one one-for-one eight-u8 appearance sidecar while preserving
  the width-one 256-star atlas and exact four-u32 active-site stream.
- Faces use their direct solid sample.  Bands and junctions use stable incident
  sets reduced by arithmetic mean for material and componentwise maximum for
  light; exhaustive tests close equivariance over the full cubical group.
- Earth supplies green top, soil side, and darker underside tones; a single
  cardinal exposure rule reveals existing highland rock below the living cap;
  authored sanctuary cells remain pale limestone.
- Both shader backends compile, the shadow path binds no appearance, repainting
  preserves geometry bytes and triangle counts, the relevant broader suite is
  green, and the inspected live frame retains coherent sun, shadows, bands,
  and junctions.  #H1ASQU is next and has not begun.

** 2026-08-28 — The static selector hypothesis failed constructively

- #33UIQ6 now closes with an exhaustive executable stabilizer proof rather
  than metadata that would encode arbitrary orientation choices.
- The proof checks all 4,200 emitted triangle primitives under the full
  48-element cubical group and preserves both the atlas census and exact
  four-word active-site fixture.
- #3UTRKA revises the appearance decision from distinguished samples to
  symmetry-stable sample sets with a commutative junction reduction; #FQ5007
  records the counterexample and commands.
- No geometry, active-site production, renderer resource, shader, material,
  world, or voxel-light rendering work began.  #FTZ8FD is next.

** 2026-08-28 — The large-world effort gained a durable execution spine

- #59ZKO4 separates the intended authored journey from terrain noise and from
  speculative engine generality.
- #94AJDM records the geometry, content, residency, publication, and lighting
  invariants inherited from the simplification and the Luvcraft substrate.
- #N3DRNE sequences five review gates from atlas semantics through the first
  durable journey.
- #3UTRKA preserves the initial architectural choices; #FQ5007 records the
  repository survey and Oracle consultation that motivated them.

* Exact next action
:PROPERTIES:
:ID: MTAAPI
:END:

The architectural parent reviews #FTZ8FD at implementation revision
=2948176327086d6177b4ac6109ec6585078a6493=: the eight-u8 sidecar, full-group
sample-set proof, commutative reduction, geometry-preserving repaint fixture,
geometry-only shadow path, both shader backends, broader tests, and inspected
sanctuary frame.  If accepted, begin #H1ASQU in a fresh execution thread from
the integrated parent revision: materialize the first deterministic authored
demand world with immutable resident chunks, sparse overlay, camera demand,
incarnation validation, and bounded eviction.

Do not begin regional voxel light, persistence, transparency, LoD, or vertical
streaming during #H1ASQU, and do not replace the appearance sidecar with face
stocks or material-dependent geometry.
